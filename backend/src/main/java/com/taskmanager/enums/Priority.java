package com.taskmanager.enums;

// Enum for task priority level
// Helps users sort and identify which tasks are most important
public enum Priority {
    LOW,    // Not urgent, can be done later
    MEDIUM, // Should be done soon
    HIGH    // Urgent, needs attention right away
}
